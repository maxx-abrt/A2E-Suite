{
  "name": "A2E Suite — executor batch 11 (2026-09-24, base 72c38486)",
  "description": "Batch 11: orchestrator-queued M1 defects (US-080 upgrade cursor reachability, US-081 install log hygiene) plus dependency-ready Tier-0 legs extracted from PLAN.md. US-080..US-085 verified by orchestrator 2026-09-24 (unit/integration/tsgo + live /calendar Tier-2); passes flipped true. Live Tier-2 pass also found P4.2 defects: task-calendar GroupByTasks complexity > cap (renders empty), project Tâches tab unscoped, project Aperçu overview widget empty — see phase-04-report 20:52 CEST.",
  "branchName": "main",
  "userStories": [
    {
      "id": "US-074",
      "title": "Fix a2e-projects install-time workspace-schema build failure",
      "priority": 1,
      "passes": true,
      "dependsOn": [],
      "completionNotes": "Done. Commit abed30a3. augment-flat-entity-maps-with-relation-targets.util.ts. Verified live by orchestrator."
    },
    {
      "id": "US-075",
      "title": "Fix CRM nav-restore defect + user-hidden-item provenance",
      "priority": 2,
      "passes": true,
      "dependsOn": [],
      "completionNotes": "Done. Commit 50a931ca + provenance follow-up. Verified live by orchestrator."
    },
    {
      "id": "US-076",
      "title": "Fix INSTALL_APP step idempotency — skip succeeded when app already installed at compatible version",
      "priority": 1,
      "passes": true,
      "dependsOn": [],
      "completionNotes": "Done. Commit 23d1b6d2. installTemplateApplication now checks findByUniversalIdentifier before calling installApplication. 2 regression tests added. Tier-2 residual: live STUDENT-apply on workspace with A2E Documents already installed."
    },
    {
      "id": "US-077",
      "title": "Add a2e-projects/chat/drive to A2E allowlist (A2eSuiteApplicationUniversalIdentifiers.ts) and OnboardingInstallableApps.ts",
      "description": "As a user viewing Settings → Applications, I want to see all 5 A2E Suite apps (Documents, Bilan, Projects, Chat, Drive) in the A2E section once they are registered, so that I can install/manage any of them from one place.",
      "priority": 1,
      "passes": true,
      "dependsOn": [],
      "completionNotes": "Done. Commit 5bfe5bef. Added Projects (4f759655), Chat (e2dce399), Drive (b11cd01f) to A2eSuiteApplicationUniversalIdentifiers.ts and OnboardingInstallableApps.ts with proper Lingui labels and descriptions. docs/applications.md corrected. Tier-2 residual: with apps registered on a live server, verify Settings → Applications → A2E Suite shows all 5."
    },
    {
      "id": "US-078",
      "title": "M1 Dockerfile — add twenty-apps-build stage building all 5 A2E apps into tarballs",
      "description": "As an operator deploying A2E Suite via Docker, I want the production image to contain built tarballs for all 5 A2E apps so that a fresh deploy automatically registers and installs them without manual app:publish rituals.",
      "priority": 1,
      "passes": true,
      "dependsOn": [],
      "completionNotes": "Done. Commit 5bfe5bef. New twenty-apps-build multi-stage: yarn install + yarn twenty dev:build --tarball for each of the 5 internal apps; tarballs collected at /app/packages/twenty-apps/dist in the server image. CI workflow (cd-docker-image.yaml) already uses GHA cache and will build this stage automatically. Tier-2 residual: docker build must complete and /app/packages/twenty-apps/dist must contain 5 tarballs in the image."
    },
    {
      "id": "US-079",
      "title": "M1 startup registration — ProvisionBundledAppsCommand (app:provision-bundled) + BUNDLED source type + entrypoint.sh integration",
      "description": "As an operator on a fresh Docker deployment, I want the server to automatically register all bundled A2E app tarballs at startup and install them on all workspaces so that Settings → Applications shows the A2E section without manual CLI work.",
      "priority": 1,
      "passes": true,
      "dependsOn": [
        "US-078"
      ],
      "completionNotes": "Done. Commits 5bfe5bef + a437674f. ApplicationRegistrationSourceType.BUNDLED enum value; ApplicationRegistrationEntity.bundledAppSourcePath nullable text column with @WasIntroducedInUpgrade; 2-39 instance command (1789905000000) ADD COLUMN; ApplicationPackageFetcherService BUNDLED case resolves from FS path; ProvisionBundledAppsCommand (app:provision-bundled): reads *.tgz from BUNDLED_APPS_DIR, registers with sourceType=BUNDLED/isPreInstalled=true/ownerWorkspaceId=null, idempotent, --dry-run; DatabaseCommandModule wired; entrypoint.sh calls provision_bundled_apps() after setup_and_migrate_db (provision-bundled then install-pre-installed-apps); PreInstalledAppsService.installOnWorkspace improved (APP_ALREADY_INSTALLED no longer error-logs); 5-case spec file. Tier-2 residual: live Docker boot registering 5 apps + installing on all workspaces."
    },
    {
      "id": "US-080",
      "title": "M1(e) upgrade cursor reachability — resume at an instance command inserted behind an applied workspace segment",
      "description": "As an operator upgrading an instance already at the 2.39.0 workspace segment to an image carrying the new AddBundledAppSourcePath instance command, I want `upgrade` to run that instance command once without re-running the applied workspace segment, so that the bundled-app column exists and boot stays idempotent.",
      "priority": 1,
      "passes": true,
      "dependsOn": [
        "US-079"
      ],
      "completionNotes": "Executor done (awaiting orchestrator). UpgradeSequenceRunnerService.resolveStartCursor: workspace last-attempted cursor now resumes at the first unattempted instance command of the instance block preceding the workspace segment (getLastAttemptedInstanceCommand index + 1; whole block when the instance cursor is before it) instead of the segment start. Second defect found while fixing: running such a late instance command wrote workspace-scoped rows that became each workspace's newest cursor, re-queuing the entire applied 2.39.0 workspace segment on the next boot — InstanceCommandRunnerService now skips workspace rows for workspaces whose cursor is already past the step (workspaceIdsPastThisStep). Barrier enforcement accepts cursors past the barrier (also fixes retrying a failed instance step that directly follows a workspace segment, which previously threw). Specs: unit upgrade-sequence-runner-inserted-instance-command.spec.ts (6 cases, mocked services), exclude-workspace-ids.util.spec.ts (3), integration inserted-instance-command.integration-spec.ts (4 cases incl. second-boot no-op). No committed 2-39 file touched, no renumbering. Tier-2 residual: on the dev DB with both workspaces at index 328, `upgrade` runs AddBundledAppSourcePath once (instance row only) and a second `upgrade` is a no-op."
    },
    {
      "id": "US-081",
      "title": "Install log hygiene — refuse an already-installed version before any state change (no ERROR on every boot)",
      "description": "As an operator watching boot logs, I want `install-pre-installed-apps` to report already-installed bundled apps at info level only, so that real install failures are not buried under one ERROR per app per workspace per boot.",
      "priority": 2,
      "passes": true,
      "dependsOn": [
        "US-079"
      ],
      "completionNotes": "Executor done (awaiting orchestrator). ApplicationInstallService.runInstall: for an INSTALLED application the version-progression check now runs before the UPGRADING state flip and outside the rollback try/catch (new private assertInstallVersionProgressionAllowed). SAME_VERSION → APP_ALREADY_INSTALLED thrown with no log (PreInstalledAppsService/template service keep their info-level skip line); DOWNGRADE/INVALID → one warn line; no UPGRADING→INSTALLED flip-flop, no revertStateToInstalledBestEffort. runInstallWithMetrics no longer counts APP_ALREADY_INSTALLED as app-upgrade/failed. Same exception message/code → failing-application-version-progression integration snapshot unchanged. New unit spec application-install.service.already-installed.spec.ts (3 cases: same version silent refusal, downgrade warn+metric, newer version still UPGRADING→INSTALLED). Tier-2 residual: boot/`install-pre-installed-apps` on a workspace with the bundled apps already installed shows only the PreInstalledAppsService info line."
    },
    {
      "id": "US-082",
      "title": "P4C.3 view-level occurrence expansion on /calendar (anchor rows expand into occurrences for the visible range)",
      "description": "As a calendar user, I want a recurring event to show on every day its rule produces, so that the calendar reflects the series instead of only the anchor and detached rows.",
      "priority": 1,
      "passes": true,
      "dependsOn": [],
      "completionNotes": "Executor done (awaiting orchestrator). expandCalendarEventsForRange (single-sourced engine, series zone / UTC for all-day, skipped + detached exclusion, deterministic occurrence ids, occurrenceSourceByEventId back-map); getCalendarSeriesEvents resolves unstamped anchors to the derived series id; CalendarPage separates displayed vs stored event for the scope planners. 13 new cases; front calendar 193/193 via scratch harness. Tier-2: /calendar browser proof."
    },
    {
      "id": "US-083",
      "title": "P4C.3 recurrence rule editor in the /calendar composer (create, make-recurring, whole-series edit)",
      "description": "As a calendar user, I want to set daily/weekly/monthly repetition with interval, weekdays, monthly position and an end condition when creating or editing an event, so that recurring events can be created without the API.",
      "priority": 1,
      "passes": true,
      "dependsOn": [
        "US-082"
      ],
      "completionNotes": "Executor done (awaiting orchestrator). CalendarRecurrenceFields + CalendarRecurrenceDraft utils (rule<->draft round-trip, inclusive UNTIL in series zone, clamps, rebase on start-day change); create pre-generates the anchor id so the series id is stamped in the same write; whole-series edit writes the edited rule via planCalendarSeriesEdit. 19 new cases; front calendar 212/212 via scratch harness; tsc on pure files + stubbed components/page; repo oxlint JS rules 0/0. Tier-2: /calendar create-recurring browser proof."
    },
    {
      "id": "US-084",
      "title": "P4C.5 task due-date overlay on /calendar (read-only, opens the task in the native side panel)",
      "description": "As a calendar user, I want my tasks' due dates to show on the calendar days they fall on, visibly distinct from scheduled events, so that I can see deadlines next to my schedule and open the task in one click without the calendar ever turning a deadline into an event.",
      "priority": 1,
      "passes": true,
      "dependsOn": [
        "US-082"
      ],
      "completionNotes": "Executor done (awaiting orchestrator). useCalendarTaskDueDates reads standard `task` rows (id/title/dueAt/status) with a dueAt range filter built in the viewer zone (getCalendarTaskDueQueryRange: start of first visible day inclusive, start of the day after the last exclusive; DST-aware), skipped when the overlay is hidden or the member lacks task read permission (useFindManyRecords already gates on it). groupCalendarTaskDuesByDay buckets by the due day in the viewer zone, skips null/unreadable/out-of-range due dates, dedupes, flags overdue (open + before today) vs done, orders open by due time/title and sinks done. CalendarTaskDueChip: dashed outline + IconCheckbox + Due/Overdue/Done state (never the event accent border), opens the task via useOpenRecordInSidePanel. Wired into month/week/day/agenda (agenda lists days that only have deadlines; empty states count deadlines), toolbar toggle 'Task due dates' (aria-pressed), non-blocking load error line. No write path, no schema/server change, works without Projects (dueAt is standard). 9 new cases; front calendar 21 suites / 221 tests green (scratch harness). Tier-2: /calendar browser proof."
    },
    {
      "id": "US-085",
      "title": "P4C.5 quick-create a standard task from a calendar day (day + week views)",
      "description": "As a calendar user, I want to add a task due on a given day straight from the calendar, so that I can capture a deadline where I am planning without opening the Tasks page — and without the calendar scheduling or inviting anyone.",
      "priority": 2,
      "passes": true,
      "dependsOn": [
        "US-084"
      ],
      "completionNotes": "Executor done (awaiting orchestrator). 'Add task' on the day view header and an icon button per week-view day column open CalendarTaskQuickCreateDialog (labelled title input, autofocus, Escape cancels, 'Due <full date>' line, hint that nothing is scheduled). useCalendarTaskQuickCreate writes one standard `task` through useCreateOneRecord (native record path, pre-generated id, status TODO, dueAt = local noon of the chosen day via buildCalendarQuickTaskInput so the day holds for teammates within ±11h and round-trips through the overlay grouping); blank titles never submit. Buttons only render when the deadline overlay is shown and the member can create tasks (object permission canUpdateObjectRecords). FORBIDDEN vs generic error copy; success refetches the overlay and announces 'Task created'. No event row, no invitation, no schema/server change. 8 new cases; front calendar 22 suites / 229 tests green (scratch harness). Tier-2: /calendar browser proof."
    }
  ],
  "metadata": {
    "updatedAt": "2026-09-24T04:34:54.000Z"
  }
}
