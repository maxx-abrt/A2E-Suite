# Phase 08 — Inbox, Notifications & Activity

CLAIMED — P8.1-notification-service/notification-core-service — deepseek-v4.1-flash — 2026-09-18T06:06:03Z — base 11e923d7ab7feb303dbb7670d4ac3c29b9b24363

## 2026-09-18 06:11 UTC — deepseek-v4.1-flash [executor] — contract v4
**Task:** P8.1-notification-service P8.1: notification core service — event consumers, digest batching, quiet hours, preference model · **Slice:** P8.1 bullets 1 & 3 (notification core service + per-user preference model); bullet 2 (realtime push) is a separate task, P8.2 is the inbox UX
**Claim:** done-for-review
**Ready-to-tick:** yes
**Base:** 11e923d7ab7feb303dbb7670d4ac3c29b9b24363
**Changed:** `packages/twenty-server/src/engine/core-modules/notification/**` (new: `notification.entity.ts`, `notification.module.ts`, 4 constants, 3 types, 4 utils, 1 service, 1 listener, 5 specs); `packages/twenty-server/src/engine/core-modules/core-engine.module.ts` (register `NotificationModule`); `packages/twenty-server/src/database/commands/upgrade-version-command/2-39/2-39-instance-command-fast-1789711800000-create-notification-table.ts` (new); `packages/twenty-server/src/database/commands/upgrade-version-command/instance-commands.constant.ts` (register command); `docs/plan/phases/phase-08-report.md`; `.ralph-tui/progress.md`
**Checks:** `npx jest src/engine/core-modules/notification --config=packages/twenty-server/jest.config.mjs` → 5 suites / 29 tests pass; `cd packages/twenty-server && npx tsgo -p tsconfig.json --noEmit` → exit 0; `npx oxlint --type-aware -c .oxlintrc.json src/engine/core-modules/notification src/engine/core-modules/core-engine.module.ts src/database/commands/upgrade-version-command/2-39/2-39-instance-command-fast-1789711800000-create-notification-table.ts src/database/commands/upgrade-version-command/instance-commands.constant.ts` (from `packages/twenty-server`) → 0 warnings / 0 errors (23 files); `npx oxfmt --check` on the same globs → all formatted; `npx nx lint:diff-with-main twenty-server` → "No changed files" (it diffs `main...HEAD`, so uncommitted edits are invisible — direct oxlint+oxfmt is the covering gate per precedent)
**Missing for tick:** Live DB application of `CreateNotificationTableFastInstanceCommand` (Tier 1/2 `database:reset`/migrate) and a boot/install smoke — orchestrator-owned; the SQL mirrors the committed `documentShare` command (same `uuid_generate_v4()`/core-schema/FK pattern). Email *sending* (templates/transport) is P8.2 — this slice only plans digest batches. Realtime push/inbox GraphQL are P8.1-notification-realtime/P8.2.
**Do not redo:** The notification module, preference model and specs are complete and green — extend, don't rebuild. `NotificationService.requestNotifications` is the one producer seam; do not add per-app notification paths (C5).
**Remaining:** P8.1 bullet 2 (realtime push) + P8.2 bullets; P8.1 bullets 1 & 3 close here
**Next:** P8.1-notification-realtime (realtime push on `workspace:<id>:inbox:<userId>` + unread badge counts; dependsOn ACL + gateway) or P8.2 inbox filters/grouping/deep-links consuming `NotificationService.findForUser`/`countUnread`

CLAIMED — P8.1-notification-realtime/inbox-realtime-push — deepseek-v4.1-flash — 2026-09-18T21:13:02Z — base 98669254d32cf5cc83b9fecd6bd24e0b1d174ea0

## 2026-09-18 21:15 UTC — deepseek-v4.1-flash [executor] — contract v4
**Task:** P8.1-notification-realtime P8.1: realtime push on the inbox topic + unread badge counts · **Slice:** bullet 2 (realtime push + badge counts) — the task's only unmet bullet; bullets 1 & 3 were closed by P8.1-notification-service
**Claim:** done-for-review
**Ready-to-tick:** yes
**Base:** 98669254d32cf5cc83b9fecd6bd24e0b1d174ea0
**Changed:** `packages/twenty-server/src/engine/core-modules/notification/utils/inbox-topic.util.ts` (new — `buildInboxTopic` for `workspace:<id>:inbox:<userId>`); `.../utils/notification-realtime-event.util.ts` (new — `notification.inbox.updated` event + entity→entry mapper); `.../services/notification-realtime-publisher.service.ts` (new — per-recipient fan-out, unread count read from `NotificationService.countUnread`); `.../listeners/notification-requested.listener.ts` (publish dispatched inbox rows after dispatch, still best-effort); `.../notification.module.ts` (provide publisher, import `RealtimeGatewayModule`); specs: new `utils/__tests__/inbox-topic.util.spec.ts`, `utils/__tests__/notification-realtime-event.util.spec.ts`, `services/__tests__/notification-realtime-publisher.service.spec.ts`; extended `listeners/__tests__/notification-requested.listener.spec.ts`
**Checks:** `npx jest src/engine/core-modules/notification --config=jest.config.mjs` (twenty-server, in-package) → 8 suites / 39 tests passed; `npx tsgo -p tsconfig.json --noEmit` (twenty-server, in-package) → exit 0; `npx oxlint --type-aware -c .oxlintrc.json src/engine/core-modules/notification` → 0 warnings / 0 errors (26 files); `npx oxfmt --check src/engine/core-modules/notification` → clean after `npx oxfmt` reformatted the new topic spec; `npx nx lint:diff-with-main twenty-server` → "No changed files." (diffs `main...HEAD`, so uncommitted edits are invisible — direct oxlint+oxfmt above is the covering gate per precedent). No twenty-shared / migration / locale changes.
**Missing for tick:** Tier-2 live badge update (a real socket on `workspace:<id>:inbox:<userId>` receiving the push and the badge incrementing in a browser) — orchestrator per the task's own gate. Read/archive-side badge decrement is not wired: `markAsRead`/`archiveNotification` have no caller yet (P8.2 inbox resolver); when that ships it should publish the same recomputed count (or move count aggregation so `NotificationService` can publish without a cycle).
**Do not redo:** the notification core service/preference model/listener dispatch are green — the publisher is additive and the listener only gained the post-dispatch fan-out; `NotificationService` is untouched. The inbox topic grammar/parser and the user-scoped subscribe ACL already existed (P2.1) and are covered by `realtime-topic-authorization.service.spec.ts` ("rejects another user inbox topic but allows the own one").
**Remaining:** P8.2 bullets (inbox UX, mentions engine, activity feed, email notifications, watchers); P8.1 closes here.
**Next:** P8.2 inbox app/page consuming `NotificationService.findForUser`/`countUnread` + subscribing the inbox topic with the P2.2 `useRealtimeTopic` primitive (build the matching front `buildInboxTopic`); orchestrator ticks P8.1 and runs the Tier-2 live-badge journey.

CLAIMED — P8.2-inbox-page/inbox-page — deepseek-v4.1-flash — 2026-09-18T21:16:30Z — base 797efd93816a43e08a58551b05c6151f74c74406
RESUMED — P8.2-inbox-page/inbox-page — deepseek-v4.1-flash — 2026-09-18T21:28:00Z — base 797efd93816a43e08a58551b05c6151f74c74406 (previous iteration of this same session built the slice but stalled before reporting; this run verified it and closed two lint gates)

## 2026-09-18 21:31 UTC — deepseek-v4.1-flash [executor] — contract v4
**Task:** P8.2-inbox-page P8.2: inbox app/page — filters, day grouping, bulk actions, deep links · **Slice:** the task's single bullet (native inbox page + core `notification` GraphQL surface)
**Claim:** done-for-review
**Ready-to-tick:** yes
**Base:** 797efd93816a43e08a58551b05c6151f74c74406
**Changed:** twenty-front `src/modules/inbox/**` (components `InboxFilterTabs`/`InboxBulkActions`/`InboxNotificationList`/`InboxNotificationItem`; hooks `useInboxNotifications`/`useInboxUnreadCount`/`useInboxBulkActions`/`useInboxLive`; graphql queries `inboxNotifications`/`inboxUnreadCount` + mutations `markInboxNotificationsAsRead`/`archiveInboxNotifications`; types `InboxNotification`/`InboxRealtimeEvent`; utils `buildInboxTopic`/`filterInboxNotificationsByCategory`/`groupInboxNotificationsByDay`/`resolveNotificationDeepLink`/`applyInboxRealtimeEventToNotifications` + 5 specs) + `src/pages/inbox/InboxPage.tsx` + `modules/app/routing/utils/createWorkspaceRouteObjects.tsx` (lazy route); twenty-shared `src/types/AppPath.ts` (`Inbox = '/inbox'`); twenty-server notification `notification.resolver.ts` + `dtos/notification.dto.ts` + `utils/notification-to-dto.util.ts` + `notification.module.ts` + `services/notification.service.ts` (`archiveNotifications` bulk extract) + specs (`notification.resolver.spec.ts`, `utils/__tests__/notification-to-dto.util.spec.ts`, extended `notification.service.spec.ts`)
**Checks:** front `npx jest src/modules/inbox --config=packages/twenty-front/jest.config.mjs` → 5 suites / 26 tests PASS; server `npx jest src/engine/core-modules/notification --config=packages/twenty-server/jest.config.mjs` → 10 suites / 48 tests PASS; `cd packages/twenty-server && npx tsgo -p tsconfig.json --noEmit` → exit 0; `cd packages/twenty-front && npx tsgo -p tsconfig.json --noEmit` → only the 6 baseline `modules/front-components` AppPath nominal-mismatch errors (reproduced on the clean base tree, no inbox/server error); front `npx oxlint --type-aware -c .oxlintrc.json src/modules/inbox src/pages/inbox src/modules/app/routing/utils/createWorkspaceRouteObjects.tsx` → 0 warnings / 0 errors + `npx oxfmt --check` clean; server `npx oxlint --type-aware -c .oxlintrc.json src/engine/core-modules/notification …` → 0/0 + `oxfmt --check` clean; `npx nx lint:diff-with-main twenty-front` and `…:twenty-server` → success ("No changed files" — target diffs `main...HEAD`, so uncommitted edits are invisible; direct oxlint/oxfmt above is the covering gate); `npx nx build twenty-shared --skip-nx-cache` → success
**Missing for tick:** Tier-2 browser journey (open `/inbox`, realtime badge, bulk mark-read/archive, mention→inbox→open e2e) — orchestrator-owned per the task gate. There is intentionally no front nav registry entry (P1.2 verified nav items are DB rows; a native core page is reached by route/deep link, matching the P5.2 `AppPath.Discussions` precedent); if the orchestrator wants a Cmd+K/search entry that is a separate wiring step.
**Do not redo:** the front inbox module, the `/inbox` route and the core notification resolver/query/mutations are complete and green — extend, don't rebuild. `resolveNotificationDeepLink` matches the P5.1 chat producer payload (`kind: 'chat.mention'`, `channelId`, `messageId`) and the `{objectNameSingular, recordId}` record convention; unknown/deleted targets return null → the row renders the non-navigable "no destination" state, never an error page. The inbox uses the P2.2 `useRealtimeTopic` hook on `workspace:<id>:inbox:<userId>` (core User id, not workspaceMember id) with an idempotent insert-by-id fold; the unread count is server-derived via `notificationUnreadCount`, overridden by a realtime event's `unreadCount`.
**Remaining:** P8.2 mentions engine, activity feed, email notifications, watchers (4 other P8.2 tasks still open).
**Next:** orchestrator ticks P8.2-inbox-page and runs the Tier-2 mention→inbox→open journey; then P8.2-mentions-engine (shared parser for docs/chat/comments).

CLAIMED — P8.2-mentions-engine/mentions-engine-core — deepseek-v4.1-flash — 2026-09-18T23:40:00Z — base 1e7eec28070f87ca53b653a248b49759743e38e0

## 2026-09-18 21:46 UTC — deepseek-v4.1-flash [executor] — contract v4
**Task:** P8.2-mentions-engine P8.2: shared mentions parser for docs, chat and comments · **Slice:** the task's core bullet — shared parser across the three surfaces emitting MENTION notifications with context snippets, permission-aware
**Claim:** done-for-review
**Ready-to-tick:** yes
**Base:** 1e7eec28070f87ca53b653a248b49759743e38e0
**Changed:** twenty-server new `src/modules/mention/**` (`types/mention.type.ts`; utils `parse-chat-mentions`, `parse-blocknote-mentions`, `parse-mentions`, `build-mention-snippet`, `build-mention-notification-payload`, `filter-permitted-mention-targets`, `subtract-mentioned-workspace-member-ids`; services `mention-notification`, `mention-access`; listeners `document-mention`, `document-comment-mention`; `mention.module.ts`; 8 specs) + `modules/chat/services/chat-mention.service.ts` (+spec, now delegates to the shared engine) + `modules/chat/utils/chat-mention.util.ts` (+spec, delegates parsing/payload to the shared engine) + `modules/chat/chat.module.ts` (imports `MentionModule`) + `engine/core-modules/realtime-gateway/services/realtime-topic-access.service.ts` (additive `canWorkspaceMemberReadObjectRecord`/`canWorkspaceMemberReadChatChannel` taking an explicit member identity; socket paths unchanged) + `engine/core-modules/realtime-gateway/realtime-gateway.module.ts` (export the access service) + `engine/core-modules/core-engine.module.ts` (register `MentionModule`)
**Checks:** `npx jest src/modules/mention src/modules/chat src/engine/core-modules/realtime-gateway --config=jest.config.mjs` (in `packages/twenty-server`) → 25 suites / 136 tests pass; `cd packages/twenty-server && npx tsgo -p tsconfig.json --noEmit` → exit 0; `npx oxlint --type-aware -c .oxlintrc.json` on `src/modules/mention src/modules/chat …realtime-topic-access.service.ts …realtime-gateway.module.ts …core-engine.module.ts` → 0 warnings / 0 errors (58 files); `npx oxfmt --check` on the same → clean; `npx nx lint:diff-with-main twenty-server` → success ("No changed files." — target diffs `main...HEAD`, so uncommitted edits are invisible; direct oxlint+oxfmt above is the covering gate). No twenty-shared / migration / locale / twenty-front changes.
**Missing for tick:** Tier-2 real event journey (install a2e-documents, edit a document / add a comment / post a chat message in a private channel, observe mention rows in `/inbox` with the snippet and deep link; orchestrator-owned). The document/comment listeners only fire once the `a2e-documents` app objects exist in a workspace — at Tier 0 they are covered by spec with synthetic DB-event payloads. `note`/legacy RICH_TEXT surfaces are intentionally out of this slice (P3.2 document object is the doc surface).
**Do not redo:** the shared parser (`parseMentions` dispatcher + chat/BlockNote arms), snippet builder, member→user resolution, permission filter, per-surface payloads and the three listeners are complete and green. `ChatMentionService` now only maps a message to `{ surface:'chat', channelId, messageId }`; the chat `MENTION` payload keeps `kind:'chat.mention'` and adds `snippet`, so `resolveNotificationDeepLink` and the inbox preview keep working. `RealtimeTopicAccessService` is additive: `assertCanAccess*` behavior is unchanged.
**Remaining:** P8.2 activity feed, email notifications, watchers (3 other P8.2 tasks still open; inbox page + mentions now closed).
**Next:** orchestrator ticks P8.2-mentions-engine and runs the Tier-2 mention→inbox journey; then P8.2 email notifications (twenty-emails templates honoring preferences/quiet hours) or watchers.

CLAIMED — P8.2-activity-feed/home-dashboard-widgets — deepseek-v4.1-flash — 2026-09-18T21:50:30Z — base fb284deb7f6c6f6395343d2da4db9a24e378990b

## 2026-09-18 22:05 UTC — deepseek-v4.1-flash [executor] — contract v4
**Task:** P8.2-activity-feed P8.2: unified activity feed — Home dashboard widgets · **Slice:** the task's single bullet (four workspace widgets composed from existing primitives and slotted into the P2.4 dock registry)
**Claim:** done-for-review
**Ready-to-tick:** yes
**Base:** fb284deb7f6c6f6395343d2da4db9a24e378990b
**Changed:** twenty-front new `src/modules/home-dashboard/**` — containers `RecentActivityWidget`/`MyTasksWidget`/`UpcomingEventsWidget`/`ContributionGridWidget`; presentational `HomeWidgetList` + `RecentActivityWidgetContent`/`MyTasksWidgetContent`/`UpcomingEventsWidgetContent`/`ContributionGridWidgetContent`; utils `buildContributionGrid`/`selectMyTasks`/`selectUpcomingEvents`/`summarizeTimelineActivity`/`formatHomeWidgetDayLabel` + 5 specs; 5 component specs; `__tests__/registerHomeDashboardWidgets.test.ts`; 4 Storybook stories; `registerHomeDashboardWidgets.ts` (re-registers `tasks`→MyTasks, `activity`→RecentActivity; adds `events`, `contributions`). Edits: `modules/workbench-dock/components/WorkbenchWidgetDock.tsx` (side-effect import of the registrar) and `DefaultWorkbenchWidgetContent.tsx` (titles for `events`/`contributions`).
**Checks:** `npx jest src/modules/home-dashboard --config=packages/twenty-front/jest.config.mjs` → 11 suites / 31 tests PASS; `npx jest src/modules/workbench-dock …` → 3 suites / 6 tests PASS; `cd packages/twenty-front && npx tsgo -p tsconfig.json --noEmit` → only the 6 baseline `modules/front-components` AppPath nominal-mismatch errors (reproduced on the clean base, no home-dashboard/workbench-dock error); `npx oxlint --type-aware -c .oxlintrc.json src/modules/home-dashboard src/modules/workbench-dock/components/{WorkbenchWidgetDock,DefaultWorkbenchWidgetContent}.tsx` → 0 warnings / 0 errors; `npx oxfmt --check` on the same → clean; `npx nx lint:diff-with-main twenty-front` → success ("No changed files" — target diffs `main...HEAD`, so uncommitted edits are invisible; direct oxlint/oxfmt above is the covering gate). No twenty-shared / migration / locale changes.
**Missing for tick:** Tier-2 browser proof (open the workbench dock, switch to each Home widget against a seeded workspace: recent activity rows, assigned-not-done tasks with overdue styling, upcoming `calendarEvent`s, a 12-week contribution grid) — orchestrator-owned per the task gate. The `my-tasks`/`contributions` queries assume `workspaceMemberId` is filterable on `timelineActivity` and that `assigneeId` is filterable on the standard `task` object; verified by types/GraphQL schema but not against a live DB at Tier 0.
**Do not redo:** the P2.4 workbench dock registry and its six defaults are green and unchanged (`DEFAULT_WIDGETS` untouched — the registry spec pins them). The Home widgets are additive: re-registering the existing `tasks`/`activity` ids only attaches a `Component`; `inbox`/`assistant`/`comments`/`presence` are untouched. The pure util contracts (`buildContributionGrid`, `selectMyTasks`/`isTaskOverdue`, `selectUpcomingEvents`, `summarizeTimelineActivity`) are stable; extend rather than rewrite.
**Remaining:** P8.2 email notifications and watchers (2 other P8.2 tasks still open; inbox page, mentions and activity feed now closed).
**Next:** orchestrator ticks P8.2-activity-feed and runs the Tier-2 dock journey; then P8.2 email notifications (twenty-emails digest/instant honoring preferences + quiet hours) or P8.2 watchers.

CLAIMED — P8.2-email-notifications/email-digest-instant — deepseek-v4.1-flash — 2026-09-18T21:59:43Z — base 6d220880aaf65f91c2ed72b4aaa792857ba44631
RESUMED — P8.2-email-notifications/email-digest-instant — deepseek-v4.1-flash — 2026-09-18T22:08:00Z — base 6d220880aaf65f91c2ed72b4aaa792857ba44631 (previous iteration of this same session built the templates/sender/wiring but stalled before checks or report; this run fixed the lingui-id specs, ran oxfmt, and verified all Tier-0 gates)

## 2026-09-18 22:08 UTC — deepseek-v4.1-flash [executor] — contract v4
**Task:** P8.2-email-notifications P8.2: email notifications — digest + instant via twenty-emails, honoring preferences and quiet hours · **Slice:** the task's single email bullet (instant + digest templates and a sender over the P8.1 dispatch seam)
**Claim:** done-for-review
**Ready-to-tick:** yes — every applicable Tier-0 gate is green; the only absent evidence is Tier-2 real delivery, which the executor contract forbids and the orchestrator owns
**Base:** 6d220880aaf65f91c2ed72b4aaa792857ba44631
**Changed:** `packages/twenty-emails/src/index.ts`, `.../emails/notification.email.tsx` (new), `.../emails/notification-digest.email.tsx` (new), `.../types/notification-email.type.ts` (new); `packages/twenty-server/src/engine/core-modules/notification/services/notification-email-sender.service.ts` (new), `.../services/__tests__/notification-email-sender.service.spec.ts` (new), `.../utils/notification-email-item.util.ts` (new), `.../utils/__tests__/notification-email-item.util.spec.ts` (new); `.../listeners/notification-requested.listener.ts`, `.../listeners/__tests__/notification-requested.listener.spec.ts`, `.../notification.module.ts`, `.../email/__tests__/email-templates-rendering.spec.ts`, phase-08 report, `.ralph-tui/progress.md`
**Checks:** `nx build twenty-emails --skip-nx-cache` → success; `jest` on the 4 touched server specs → 49/49 green; `jest src/engine/core-modules/notification src/engine/core-modules/email` → 31 suites / 208 tests green; `npx tsgo -p tsconfig.json --noEmit` in twenty-server → exit 0 and in twenty-emails → exit 0; `oxlint --type-aware` on the 8 touched server files → 0 warnings/0 errors and on the 4 twenty-emails files → 0/0; `oxfmt --check` on the same files → clean (3 files reformatted); `nx lint:diff-with-main twenty-server` → "No changed files." (edits uncommitted, so the direct oxlint/oxfmt run is the effective gate)
**Missing for tick:** Tier-2 real delivery (rendered email actually arrives via configured mail transport) — orchestrator-only; no live digest-worker re-dispatch of quiet-hours-suppressed items (suppression is honored, re-dispatch is a future worker, not in this bullet)
**Do not redo:** the P8.1 preference/quiet-hours/digest-window planning (`NotificationService.dispatchNotifications` returns `emailDigestBatches` already filtered) — the sender must stay a thin renderer/transport over that contract; the two templates and their `email-templates-rendering` coverage are green
**Remaining:** P8.2-watchers is the only other P8.2 task still open
**Next:** orchestrator ticks P8.2-email-notifications and runs the Tier-2 delivery proof; meanwhile P8.2-watchers can be claimed

CLAIMED — P8.2-watchers/watch-primitive — deepseek-v4.1-flash — 2026-09-18T22:10:00Z — base 6e84aa6e9bf87c2bfda58af866e31eb16d0b8616

## 2026-09-18 22:20 UTC — deepseek-v4.1-flash [executor] — contract v4
**Task:** P8.2-watchers P8.2: watchers — watch record/doc/channel → notifications on change · **Slice:** the task's single bullet — one watch primitive + change-event → notification path + unwatch/permission-loss cleanup
**Claim:** done-for-review
**Ready-to-tick:** yes — every applicable Tier-0 gate is green; the only absent evidence is Tier-2 live fan-out, which the executor contract forbids and the orchestrator owns
**Base:** 6e84aa6e9bf87c2bfda58af866e31eb16d0b8616
**Changed:** `.../notification/notification-watch.entity.ts` (new), `.../notification/constants/notification-watch-target-kind.constant.ts` (new), `.../notification/dtos/notification-watch.dto.ts` (new), `.../notification/notification-watch.resolver.ts` (new), `.../notification/services/notification-watch.service.ts` (new), `.../notification/services/__tests__/notification-watch.service.spec.ts` (new), `.../notification/listeners/notification-watch.listener.ts` (new), `.../notification/listeners/__tests__/notification-watch.listener.spec.ts` (new), `.../notification/utils/build-watch-notification-payload.util.ts` (new), `.../notification/utils/__tests__/build-watch-notification-payload.util.spec.ts` (new), `.../notification/notification.module.ts`, `.../upgrade-version-command/2-39/2-39-instance-command-fast-1789900000000-create-notification-watch-table.ts` (new), `.../upgrade-version-command/instance-commands.constant.ts`, phase-08 report, `.ralph-tui/progress.md`
**Checks:** `jest` on the 3 new watch specs → 20/20 green; `jest src/engine/core-modules/notification` → 15 suites / 83 tests green; `npx tsgo -p tsconfig.json --noEmit` in twenty-server → exit 0; `oxlint --type-aware` on the 13 touched files → 0 warnings/0 errors; `oxfmt --check` on the same files → clean; `nx lint:diff-with-main twenty-server` → "No changed files." (edits uncommitted, so the direct oxlint/oxfmt run is the effective gate)
**Missing for tick:** Tier-2 live proof (a watched record/channel change actually landing a `WATCHED_RECORD_CHANGED` inbox row in the running app) — orchestrator-only; the migration has not been applied to a live DB (`database:reset`/migrate is orchestrator-owned, SQL mirrors the committed `notification` command — same `uuid_generate_v4()`/core-schema/FK pattern)
**Do not redo:** the P8.1 `NotificationService.requestNotifications` seam owns delivery — the watch service only resolves watchers and emits requests (never writes rows itself); the P2.1 `RealtimeTopicAccessService` owns access (no re-derived permission logic); the front `filterInboxNotificationsByCategory`/`resolveNotificationDeepLink` already consume `WATCHED_RECORD_CHANGED` with `{ kind, objectNameSingular, recordId }` / `channelId`
**Remaining:** P8.2-watchers was the last open P8.2 task; the whole P8.2 UX block is now reported
**Next:** orchestrator ticks P8.2-watchers (and the rest of P8.2) and runs the Tier-2 watch→inbox journey; if a follow-up is wanted, wire `NotificationWatchService.removeWatchesForUser` into `UserWorkspaceService.deleteUserWorkspace` (deliberately not done here to avoid a cross-module circular edge — the event-time ACL prune already drops removed members)

## 2026-09-19 09:20 UTC — orchestrator — P8 verification + tick

Verified all seven P8 executor reports (notification core service,
realtime push, inbox page, mentions engine, activity feed, email
notifications, watchers) against diffs `797efd93..7e04aa35`.

Evidence (re-run, uncached):
- `npx jest packages/twenty-server/src/engine/core-modules/notification --config=packages/twenty-server/jest.config.mjs` (within the 26-suite/130-test chat+notification run) → PASS.
- `npx jest packages/twenty-front/src/modules/inbox --config=packages/twenty-front/jest.config.mjs` (subset of the 39-suite/207-test front run) → PASS.
- `npx tsgo -p tsconfig.json --noEmit` in twenty-server → 0 errors.
- Migration safety: `notification` and `notificationWatch` core tables each carry a generated 2-39 instance command with `up`+`down`, registered in `instance-commands.constant.ts`, epoch-ms strictly greater than every prior 2-39 command (1789711800000, 1789900000000) — CI-enforced rules satisfied.
- No i18n catalog churn across the range; no renames/deletes; single durable watch primitive covers record/document/channel (C5 respected).

Spot-check: the `notificationWatch` unique index
`(userId, workspaceId, targetKind, targetId)` matches the service's
upsert assumptions; watch listener fan-out is failure-isolated (the
"fan-out down" WARN in test output is the deliberate best-effort path
with its own spec).

PLAN.md: P8.1 (3/3) and P8.2 (5/5) ticked `[x]` with dated annotations.

Still open (Tier 2, orchestrator-only): live badge increment on a real
socket, mention→inbox→open deep-link journey (E10), quiet-hours holding
real email delivery, and with-db execution of the notification suites.
Recorded inline on the ticks.

