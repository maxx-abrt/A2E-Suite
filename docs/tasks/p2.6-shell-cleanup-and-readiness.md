# Executor brief — P2.6 shell cleanup + P4/P4C/P3 readiness marathon

[PLAN.md](../../PLAN.md) · phase files: P2.6 → `docs/plan/phases/phase-02-report.md`,
P4.x/P4C.4 → `phase-04-report.md`, P3.x → `phase-03-report.md`, P1.x/P9.x →
`phase-01-report.md`, P10 → `phase-10-report.md`.

> **This brief overrides the usual "one slice per session, then stop" rule.**
> Work the queue below in order, append the executor report (PROMPT.md format)
> to the matching phase file **as soon as each slice's checks pass**, then start
> the next slice. Keep going until a Stop condition. The orchestrator ticks
> PLAN.md after verifying; you never tick or commit.

## Goal in one sentence

Advance the plan as far as possible in one session: remove the custom A2E right
dock cleanly, fix the three P4.2 blockers, then take the next
dependency-ready Tier-0/1 slice, and keep taking more.

## Read first — only these

- `AGENTS.md`, `PROMPT.md` (executor workflow + report format + Fast checks).
- `PLAN.md`: the **D-Shell** decision row, **P2.4**, **P2.6**, **P4.1**, **P4.2**
  (all bullets), **P4C.4**, **P3.2**, **P3.3**, **P10** (personal dashboard /
  guided help).
- The exact files named per item below (+ one adjacent sibling each).

## Ground rules

- Never commit; never edit PLAN.md.
- Additive only — **except** the explicitly approved removal in P2.6/D-Shell
  (the right dock). Do not remove anything else.
- Do not start the app. Live/browser checks are Tier-2 (orchestrator-only): list
  them under `Missing for tick`.
- App manifest changes use a manifest migration; server entity changes need a
  generated migration + upgrade command under the current `2-39/` dir with a
  strictly increasing epoch-ms timestamp and `up`+`down`.
- i18n: Lingui `t`/`Trans` for user-facing strings; never commit catalogs.
- Per-package gates: twenty-front `npx tsgo -p tsconfig.json --noEmit`;
  a2e-* `yarn typecheck && yarn lint && npx twenty dev:build .`;
  twenty-server `npx tsgo -p tsconfig.json --noEmit`; if `twenty-shared` is
  touched rebuild it first (`npx nx build twenty-shared --skip-nx-cache`).

---

## Queue — do in this order

### 1. P2.6 — remove the A2E workbench right dock, re-home its widgets (→ phase-02)

The rail is the A2E custom right sidebar. Product decided (D-Shell) it does not
match the native shell and must go. Exact work in `PLAN.md` **P2.6**; summary:

- Unmount `WorkbenchWidgetDock` from
  `packages/twenty-front/src/modules/ui/layout/page/components/MainAppLayoutWithSidePanel.tsx`
  and delete the `packages/twenty-front/src/modules/workbench-dock/` module
  (component, states, utils, registry, tests) + its
  `RootStackingContextZIndices.WorkbenchWidgetDock` entry.
- Remove the side-effect imports
  `@/home-dashboard/registerHomeDashboardWidgets`,
  `@/drive/registerDriveUsageWidget`, `@/first-open-help/registerFirstOpenHelpWidget`
  and the `registerHomeDashboardWidgets`/`registerDriveUsageWidget`/
  `registerFirstOpenHelpWidget` modules + tests if the registry is deleted.
- **Home widgets** (suggestions, my tasks, upcoming events, recent activity,
  focus/Pomodoro, contribution grid) must still be reachable: render them on the
  existing `AppPath.Home` surface. Today `pages/mobile-home/MobileHomePage.tsx`
  redirects on desktop — make the Home route render a responsive native widget
  grid for desktop too, reusing the existing `home-dashboard` components
  (`MyTasksWidgetContent`, `UpcomingEventsWidgetContent`, `RecentActivityWidgetContent`,
  `ContributionGridWidgetContent`, `PomodoroWidgetContent`, `HomeSuggestionsWidgetContent`).
  my-tasks / events / activity may deep-link to their native pages
  (`/objects/tasks`, `/calendar`, the activity feed) instead of duplicating lists.
- **Redundant widgets** (inbox, assistant, comments, presence) → remove; native
  surfaces already exist (notifications, AI side panel, record comments,
  side-panel presence). **Drive usage** → the Drive surface; **help** → a
  command-menu entry (keep the per-user dismissal + search behavior);
  **focus** already lives in Settings → Experience.
- Update `MainAppLayoutWithSidePanel.test.tsx` (the dock mock) and delete the
  dock/registry tests. `twenty-front` `tsgo` clean + touched module suites green.

Files to open: the `workbench-dock/` tree, `MainAppLayoutWithSidePanel.tsx`,
`RootStackingContextZIndices.ts`, the three `register*Widget*` files + tests,
`home-dashboard/components/*`, `pages/mobile-home/MobileHomePage.tsx`,
`modules/app/routing/utils/createWorkspaceRouteObjects.tsx`.

### 2. P4.2 — fix the three live defects I found (→ phase-04)

Pasted evidence (orchestrator, 2026-09-24): the task-calendar view's
`GroupByTasks` query returns
`{"subCode":"TOO_COMPLEX_QUERY","message":"Query complexity is too high… Query complexity: 2001. Maximum complexity: 2000."}`.

- **2a. Task-calendar view renders empty (complexity).** In
  `packages/twenty-apps/internal/a2e-projects/src/views/task-calendar.view.ts`
  reduce the view's group-by complexity (e.g. drop the `project` relation column
  from `fields` — title + `projectStatus` are enough for a day cell) so
  `GroupByTasks` stays under the cap; update `lib/__tests__/task-calendar.test.ts`.
  Re-publishing/re-installing to re-prove is Tier-2.
- **2b. Project `Tâches` tab is not project-scoped.** `project-tasks.view.ts`
  has no filter and the record-page `RECORD_TABLE` widget in
  `pages`… `page-layouts/project.page-layout.ts` adds no record scope, so the
  tab lists every task. Give the tab the current project's relation scope (the
  native record-context filter) so it shows only that project's tasks; keep the
  board tab's behaviour unchanged. Cover it in `project-object-integrity.test.ts`.
- **2c. Project `Aperçu` overview widget renders empty.**
  `front-components/project-overview.front-component.tsx` returns `null` because
  `useSelectedRecordIds()` is empty on the record page. Resolve the current
  record id through the correct `twenty-sdk/front-component` context (check the
  SDK's real exports before using one). If the SDK exposes no record context,
  report BLOCKED with the exact API surface and propose the smallest host change.

### 3. P4.1 — reinstall/permission tests (→ phase-04)

Add executor-side evidence for the remaining P4.1 legs: a reinstall
no-duplicate re-walk assertion (extend `lib/__tests__/project-object-integrity.test.ts`
to re-run the walk after a simulated second install) and a permission ACL spec
for the `A2E Projects default function role` (read/update/soft-delete refusals).
The live ACL proof stays Tier-2.

### 4. P4C.4 — calendar reminders (→ phase-04) — stretch, decision-aware

Build the non-UI core for configurable event reminders with **idempotent
delivery**, reschedule/cancel, on the P8 notification contract (`[x]`). Prefer a
native `notification` + scheduled job over a new provider. Quiet-hours/timezone
policy is touched by open decision **D05** — implement a documented default and
flag D05 rather than inventing product policy. If it needs a product decision,
write the smallest question in the report and move on. Do not claim provider
invites/attendees.

### 5. P3.2/P3.3 — documents gaps (→ phase-03)

Dependency-ready, executor-runnable (skip P3.4 — gated behind durable
save/share + D07):

- **Template instantiation** (`P3.2 "Complete template instantiation + gallery
  composition"`): fetch the authorized template body, remap IDs/anchors, prove
  copy independence. Files: `a2e-documents/src/lib/{instantiate-template,save-document-as-template}.ts`,
  the document browser/page.
- **Record integration** (`P3.3 "Record integration"`): implement selected-note
  body copy with source link + permission checks (record command).
- **Revision history** (`P3.2`) and **atomic save** (`P3.2`) are larger: only
  after the above, and follow the migration discipline for any new entity.

### 6. Continue autonomously

After the queue, take the next `[ ]`/`[~]` bullet in PLAN execution order whose
dependencies are `[x]` and whose remainder is Tier-0/1 (no running app). Skip
anything P7-gated (P7.0 open), decision-gated without a decision, or Tier-2-only.
One bounded slice, one report, repeat.

---

## Verify — run exactly these per slice (nothing more)

- `npx tsgo -p tsconfig.json --noEmit` in each touched package → clean.
- Touched twenty-front: `npx jest --findRelatedTests <paths> --config=packages/twenty-front/jest.config.mjs`.
- a2e-projects / a2e-documents: `yarn typecheck && yarn lint && npx twenty dev:build .`.
- server touched: `npx jest <specs> --config=packages/twenty-server/jest.config.mjs` and, schema changes, `npx nx database:reset twenty-server` only if you must.
- Docs touched: `node docs/scripts/check-docs.mjs` → PASS.
- `twenty-shared` touched: `npx nx build twenty-shared --skip-nx-cache` first.

## Then

- Append the executor report (PROMPT.md format) to the matching phase file as
  soon as each slice's checks pass — do not wait for the end.
- Do not tick PLAN.md and do not commit; the orchestrator verifies after you.

## Stop conditions — report BLOCKED, do not thrash

- Same failing gate after 2 fix attempts → report with the exact error.
- A product decision or missing host primitive would change the approach → report
  the smallest question and continue with the next queue item.
- Session ~2/3 consumed → write the current slice's report now, then stop.
