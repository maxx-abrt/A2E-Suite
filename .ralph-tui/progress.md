# Ralph Progress Log

This file tracks progress across iterations. Agents update this file
after each iteration and it's included in prompts for context.

## Codebase Patterns (Study These First)

- **a2e-* app manifest tests** live in `src/lib/__tests__/*.test.ts`, run with `node --test --experimental-strip-types`. Import each declarable module and `assert.equal(module.default.success, true)` before reading `module.default.config`; view/page-layout/widget/nav/role configs all come back on `config`. Native standard field ids for views resolve via `STANDARD_OBJECT_UNIVERSAL_IDENTIFIERS[object].fields[name].universalIdentifier` — do not import twenty-shared into the standalone app.
- **a2e-* quality gates**: `yarn test:unit` + `yarn typecheck` + `yarn lint` + `npx twenty dev:build .`. `npx nx lint:diff-with-main <app>` reports "Cannot find project" (apps are not Nx projects); format `src/lib/**` with a lib-inclusive oxfmt config because the root `.oxfmtrc.jsonc` ignores `**/lib/**`.
- **a2e-documents content-template reuse (P1.6e)**: a content template is a `_document` record with `kind: 'TEMPLATE'`; instantiation is a pure payload (`buildTemplateCopyPayload` → a fresh `DOCUMENT` record) that re-keys every blocknote block id while deliberately preserving `threadId`, and excludes relations/system fields (`id`/`parentId`/`companyId`/`personId`/`archivedAt`/`isFavorite`). Delete-safety is structural — a copy carries no template id/FK, so deleting the template cannot cascade; editing a copy cannot mutate the template (asserted in the 107-case lib suite). The app declares no attachment field on documents, so permission-aware attachment/ID remapping is N/A here (it belongs to app objects that do carry attachments). "Edit template" is a product call: editing = the TEMPLATE record's own page (banner says copies are unaffected) — do not build a second editor. Tier-0 gates: `node --test src/lib/__tests__/*.test.ts`, `npx tsc --noEmit`, `yarn lint`, `npx twenty dev:build .`.
- **a2e-projects view contract**: a KANBAN view's `mainGroupByFieldMetadataUniversalIdentifier` must be the app field id you intend (e.g. `TASK_FIELD_IDS.projectStatus`, not the native task `status`), and `groups[].fieldValue` must equal the target SELECT option *values*. Project-scope a task view with `ViewFilterOperand.IS_NOT_EMPTY` + `value: ''` on the relation field. Existing views can pass the manifest graph-walk yet group by the wrong field — assert the semantic target in a focused unit test, don't rely on reference-resolution alone.
- **a2e-projects calendar view contract**: a `ViewType.CALENDAR` view places records by `calendarFieldMetadataUniversalIdentifier` (must be the intended date field, e.g. native task `dueAt`) with `calendarLayout` (`ViewCalendarLayout.MONTH`); it renders on the native record-calendar grid and only displays — declaring one creates no provider event/invitation (P4C.5 owns sync). The existing graph walk resolves the calendar field but cannot catch a missing project scope: assert `calendarFieldMetadataUniversalIdentifier === dueAt` and the `IS_NOT_EMPTY` project filter explicitly. Task-view filter uids continue `c31b0100-0005-…`.
- **a2e-projects smart-list / nav-folder contract**: a multi-list "page" is a `NavigationMenuItemType.FOLDER` nav item (one entry, reserved position) whose children are `VIEW` nav items carrying `folderUniversalIdentifier` — there is no primitive that composes several views otherwise. "Current user" filters all share the value `JSON.stringify({ isCurrentWorkspaceMemberSelected: true, selectedRecordIds: [] })`; a native relation (`assignee`) filters directly on the field, while an ACTOR system field (`createdBy`) also needs `subFieldName: 'workspaceMemberId'`. Derived system-field ids come from `STANDARD_OBJECT_UNIVERSAL_IDENTIFIERS.task.fields.createdBy.universalIdentifier` (do not hardcode). "Overdue" = `dueAt` `ViewFilterOperand.IS_IN_PAST` with `value: ''` AND native `status` `IS_NOT` `'DONE'` (keep select-status, no custom-status object). Nav/view/filter/sort uids continue their existing families (`c31b0000-0010-…`, `c31b0100-0003-…`, `c31b0100-0005-…`, `c31b0100-0006-…`).
- **a2e-projects page-layout tab contract**: a record page tab hosts widgets; a table or kanban list is a `type: 'RECORD_TABLE'` widget with `objectUniversalIdentifier` (the view's object) + `configuration: { configurationType: 'RECORD_TABLE', viewUniversalIdentifier: <view uid> }` — the renderer (`RecordTableWidgetRenderer`) reads the referenced view's layout, so a plain TABLE view renders a table and a plain KANBAN view renders a kanban with no extra component (`WidgetType.VIEW` is rejected server-side). Native relation-scoped tabs are `type: 'FILES' | 'TASKS' | 'NOTES'` with only `configuration: { configurationType }` (no object/view needed; they read the record's morph inverse, e.g. `attachments`). Widget `viewId` becomes `viewUniversalIdentifier` in the manifest and resolves back to `viewId` on install. Uids continue the tab `c31b0200-0009-…` / widget `c31b0200-000a-…` families. App objects auto-provision the `attachments`/`timelineActivities`/`noteTargets`/`taskTargets` morph inverses, so a `FILES` widget and a `timelineActivities` Core query both resolve on the project object.
- **P6 Drive folder modeling (decided, report-only)**: model folders as a `driveFolder` app object (`name`, `parent` self-relation tree, color/icon, `position`) plus an **additive** `folderId` MANY_TO_ONE relation on the standard `attachment` object (inverse `files`, `SET_NULL`) — **not** a new target on the `attachment` `targetMorphId` morph set. The morph set means "record this file is attached to"; folder is an orthogonal, view-filterable organizational axis that coexists with a morph target. `folderId`/`sourceApp`/`starred` are organization/attribution, never permission (C5). Caveat: `folderId` only organizes `attachment` records — files stored in arbitrary FILES fields / rich-text bodies are `FileEntity` rows, not attachment records; project over both, don't build a second file system. Standard `attachment` extension has no in-tree precedent (existing app fields target person/task/company/opportunity), so confirm `dev:build`+install when implementing P6.1 bullets 2–3.
- **twenty-server unit-test async pattern**: `packages/twenty-server/jest.config.mjs` sets `fakeTimers.enableGlobally: true`, so `setImmediate`/`setTimeout` never fire inside specs — a test that must flush an async continuation started by an event handler (e.g. `handleConnection` → `message` → subscribe promise chain) flushes microtasks with a small `await Promise.resolve()` loop, not `setImmediate`. Private gateway handlers are reachable from tests via the public `handleConnection(fakeSocket, request)` seam plus `EventEmitter` sockets (`readyState = 1` so `send` is exercised).
- **onboarding install-apps auto-skip guard**: the native `InstallApps` step auto-skips (`InstallAppsAutoSkipEffect`) when the marketplace catalogue yields 0 available apps. The A2E build hosts the workspace-template picker on that same step, so auto-skipping an empty catalogue silently discards the template choice. Gate the decision with the pure `shouldAutoSkipInstallAppsStep` util (`hasTemplateChoices` holds it open whenever a picker is rendered); keep the picker passed or the discard returns. Front util tests live at `src/modules/onboarding/utils/__tests__/*.test.ts` importing via the `@/` alias.
- **blocknote version-history restore pattern**: `EditorVersionHistoryStore.addRestoreSnapshots({ currentBody, restoredBody })` is the durable "restore as new revision" seam — it delegates to `addSnapshot` for both bodies (one fire-after-emit persistence/prune path), so restoring appends the left state and the restored body instead of waiting for `useEditorVersionHistory`'s 120s interval snapshot; the panel must call it before `editor.replaceBlocks`. Never write the server directly or fork a second history store.

---


## 2026-09-18 - P4.1-project-object-validation
- Extended the manifest graph-walk (P4.1 task-field-integrity precedent) to the whole app surface: new `project-object-integrity.test.ts` walks 6 objects + 55 fields + 8 views + 1 page-layout + 2 nav items + 1 role and asserts 0 duplicate universal identifiers, 0 unresolved relation targets, 0 inverse-ownership mismatches, every view-field/sort/filter/group-by/calendar field and page-layout widget resolves, exactly one projectMember/milestone/timeEntry object, one least-privilege default role, and budget/spent stay CURRENCY-only.
- Files changed: `src/lib/__tests__/project-object-integrity.test.ts` (new, 10 tests), `docs/plan/phases/phase-04-report.md`, `.ralph-tui/progress.md`.
- **Learnings:**
  - The SDK's `STANDARD_OBJECT_UNIVERSAL_IDENTIFIERS` runtime values carry `.fields` maps (`{ fieldName: { universalIdentifier } }`), so a test can resolve view references to native task/note/company fields without importing twenty-shared. The existing task-field-integrity test cast away that shape; it is the real source of truth.
  - App package gates: `yarn test:unit`, `yarn typecheck`, `yarn lint`, `npx twenty dev:build .`; `npx nx lint:diff-with-main a2e-projects` always says "Cannot find project" (not an Nx project). Root `.oxfmtrc.jsonc` ignores `**/lib/**`, so formatting a lib test needs a lib-inclusive config copy passed with `-c`.
  - The built `.twenty/output/manifest.json` flattens page-layout tabs and view fields; a python walk of it is a cheap extra install-shape proof (0 dups, 0 unresolved target fields).
---

## 2026-09-18 - P4.2-board-view
- Audited the pre-existing `task-board.view.ts` (KANBAN on task) and found it grouped by the native task `status`, not the app `taskProjectStatus` select, with no project scoping — acceptance-invalid despite existing. Corrected the group-by target to `TASK_FIELD_IDS.projectStatus`, switched the project column to `TASK_FIELD_IDS.project`, added an `IS_NOT_EMPTY` filter on the task→project relation, and kept the TODO/IN_PROGRESS/DONE groups.
- Files changed: `src/views/task-board.view.ts`, new `src/lib/__tests__/task-board.test.ts` (5 tests), `docs/plan/phases/phase-04-report.md`, `.ralph-tui/progress.md`.
- **Learnings:**
  - A committed view can look valid (KANBAN, resolvable field references) yet violate acceptance: the existing graph walk resolves whatever field id is present, so it cannot catch "grouped by the wrong status field". Assert the semantic target (`mainGroupBy === TASK_FIELD_IDS.projectStatus`) explicitly.
  - Kanban view contract: `mainGroupByFieldMetadataUniversalIdentifier` is the group-by field; `groups[].fieldValue` must equal the target SELECT option *values* (TODO/IN_PROGRESS/DONE), not option ids. `shouldHideEmptyGroups` is optional.
  - Project scoping a task view reuses `ViewFilterOperand.IS_NOT_EMPTY` on the relation field with `value: ''` (server tooling documents `""` for IS_EMPTY/IS_NOT_EMPTY). Filter universal ids for task views continue the `c31b0100-0005-…` sequence.
---

## 2026-09-18 - P4.2-calendar-view
- Audited the pre-existing `task-calendar.view.ts` (native CALENDAR on the standard task object by native `dueAt`, MONTH layout) and found it was NOT acceptance-complete: no project scope (only `status IS_NOT DONE`), so it showed every open task workspace-wide, and the object id was hardcoded. Added the required `IS_NOT_EMPTY` + `value: ''` filter on the task→project relation (mirroring the board), switched the project column/filter to `TASK_FIELD_IDS.project` and the object to `STANDARD_OBJECT_UNIVERSAL_IDENTIFIERS.task.universalIdentifier`. View-only: no provider event/invitation.
- Files changed: `src/views/task-calendar.view.ts`, new `src/lib/__tests__/task-calendar.test.ts` (4 tests), `docs/plan/phases/phase-04-report.md`, `.ralph-tui/progress.md`.
- **Learnings:**
  - A committed CALENDAR view can resolve (graph walk green) yet violate acceptance by lacking project scope. Assert `calendarFieldMetadataUniversalIdentifier === native dueAt` + the `IS_NOT_EMPTY` project filter semantically, not just reference resolution.
  - Native task date field used for calendar placement is `20202020-fd99-40da-951b-4cb9a352fce3` (`dueAt`); task-view filter uids continue `c31b0100-0005-…` (calendar project filter = `…0005`).
  - Displaying a due date is purely a view declaration — no logic function or provider call, so the P4.3/P4C.5 boundary holds by construction.
---

## 2026-09-18 - P4.2-my-tasks-page
- Audited the pre-existing scaffolding: `my-tasks.view.ts` (assignee IS current-member, TABLE) + a position-120 VIEW nav item existed; "created by me" and "overdue" did not. Validated/extended rather than recreated: kept the assigned-to-me view (renamed «Assignées à moi») and turned the reserved position-120 nav entry into a FOLDER «Mes tâches» (id preserved) because no other primitive composes several views.
- Added smart list 2 «Créées par moi» (ACTOR `createdBy` on `workspaceMemberId` sub-field, current-member placeholder) and smart list 3 «En retard» (`dueAt IS_IN_PAST` + `status IS_NOT DONE`), plus their child VIEW nav items and a focused `my-tasks.test.ts` (5 tests). Extended the manifest graph walk's view/nav path lists and added folder-reference resolution.
- Files changed: `src/constants/universal-identifiers.ts`, `src/views/my-tasks.view.ts`, new `src/views/created-by-me.view.ts` + `src/views/overdue-tasks.view.ts`, `src/navigation-menu-items/my-tasks.navigation-menu-item.ts` (VIEW→FOLDER), new `src/navigation-menu-items/my-tasks-{assigned,created,overdue}.navigation-menu-item.ts`, new `src/lib/__tests__/my-tasks.test.ts`, `src/lib/__tests__/project-object-integrity.test.ts`, `docs/plan/phases/phase-04-report.md`, `.ralph-tui/progress.md`.
- **Learnings:**
  - `createdBy` is an ACTOR system field (`getFieldUniversalIdentifier`-derived, id `cc971806-18b5-5f1c-93f8-bedd466adf3e`), so a "created by me" view filter needs `subFieldName: 'workspaceMemberId'`; the front supports it (`getFilterOperandsForFilterableFieldType`, `turnRecordFilterIntoGqlOperationFilter` case `ACTOR`). `dueAt IS_IN_PAST` is value-less and allowed for DATE_TIME (`filterOperandsMap`).
  - A folder nav item needs no `viewUniversalIdentifier`; children point back via `folderUniversalIdentifier`. The manifest carries it through (`dev:build` output). Keep `NAVIGATION_MENU_ITEM_IDS.myTasks` as the folder id so the reserved position-120 identifier is preserved.
  - The integrity walk's `NavigationMenuItemDefinition` had no `folderUniversalIdentifier`; adding new view/nav files requires updating the hardcoded `VIEW_MODULE_PATHS` / `NAVIGATION_MENU_MODULE_PATHS` lists or the new files go unwalked.
---

## 2026-09-18 - P4.2-project-page-tabs
- Extended the project record page additively: kept Accueil (FIELDS + description + overview + time rollup) and Timeline (activity + gantt + native record tables), and added three tabs — «Tâches» (RECORD_TABLE bound to the `project-tasks` TABLE view), «Tableau» (RECORD_TABLE bound to the P4.2 board KANBAN view) and «Fichiers» (native FILES/attachments widget). Gantt stays in the existing Timeline tab (the acceptance notes it already carries the widget); the docs tab is deferred with the P4.3 project↔document relation decision and intentionally not shipped.
- Audited `project-overview.front-component.tsx` and found it only rendered key/status + task/milestone counts, so extended it to render health, milestones, members (names) and activity counts via the new pure `src/lib/project-overview.ts` projection (`formatProjectHealthLabel`, `formatWorkspaceMemberName`, `buildProjectOverviewSummary`). Centralised the project-tasks view id as `VIEW_IDS.projectTasks` and replaced the hardcoded gantt front-component id with `FRONT_COMPONENT_IDS.projectGantt`.
- Files changed: `src/page-layouts/project.page-layout.ts`, `src/front-components/project-overview.front-component.tsx`, `src/constants/universal-identifiers.ts`, `src/views/project-tasks.view.ts`, new `src/lib/project-overview.ts`, new `src/lib/__tests__/project-overview.test.ts`, `src/lib/__tests__/project-object-integrity.test.ts` (view-reference check + focused tab-set test), `docs/plan/phases/phase-04-report.md`, `.ralph-tui/progress.md`.
- **Learnings:**
  - Record-page lists reuse the view primitive through a `RECORD_TABLE` widget's `viewUniversalIdentifier` (not a bespoke component, not `WidgetType.VIEW`, which the server rejects). The manifest carries the uid and the install converts it to `viewId`; a plain KANBAN view renders as a board.
  - App objects get `attachments`/`timelineActivities`/`noteTargets`/`taskTargets` morph inverses auto-provisioned at object create (`object-system-relations-on-create` side effect), so FILES widgets and `timelineActivities` queries work on the project object without declaring anything.
  - Keep front-component sandbox helpers free of `twenty-sdk` (as `project-gantt` already was) — put only pure data/formatting in `src/lib` and assert the duplicated health labels against `field-vocabulary` in the node test rather than importing the SDK into the bundle.
---

## 2026-09-18 - P6.1-folder-modeling-spike
- Report-only deliverable: chose the additive `folderId` MANY_TO_ONE relation on the standard `attachment` object + a new `driveFolder` app object (name, `parent` self-relation tree, color/icon, position) over adding a `targetDriveFolder` morph target. Recorded existing primitives (attachment object + 7 morph targets sharing `20202020-f634-435d-ab8d-e1168b375c69`, FILES field/`FileEntity`/`FileFolder.FilesField`, native Files upload path), the P3 documents attachment pattern, the C5 attribution-not-permission boundary, limitations (folderId only covers `attachment` records, not arbitrary FILES-field files) and the honest P6 phase gate (P0.2/P0.4 [x], P1.7a [~] waiting on P1.6c [ ]).
- Files changed: `docs/plan/phases/phase-06-report.md` (new), `.ralph-tui/progress.md`.
- **Learnings:**
  - `attachment` morph targets all share one morphId (`STANDARD_OBJECTS.attachment.morphIds.targetMorphId.morphId`); app fields can extend standard objects (`defineField({ objectUniversalIdentifier })`, precedent: real-estate person / a2e-projects task), and the manifest type supports `RELATION` and `MORPH_RELATION` with an explicit `morphId` for both.
  - `driveFolder`-as-morph would overload the "record this file is attached to" axis and can't be view-filtered/grouped like a plain relation; additive relation is the additive-only, low-blast-radius choice the blueprint prefers.
  - P6 phase report file is `docs/plan/phases/phase-06-report.md` (created this session); `check-docs.mjs` only checks `MAINTAINED_DOCUMENTS`, so phase reports don't affect it.
---

## 2026-09-18 - P2.1-gateway-auth-repair
- Determined the bullet-1 auth repair already exists (commit `9ef96229`, recorded under P0.3): upgrade rejects disallowed origins via the shared `isRequestOriginAllowed` HTTP util; per-subscribe auth resolves the HttpOnly session cookie server-side through `UserSessionCookieService.extractSessionTokenFromRequest` and `UserSessionService.resolveSession`, or verifies an ACCESS-only JWT; membership is revalidated every subscribe. Did not touch production code.
- Closed the one acceptance bullet without unit coverage — "connection versus subscription success is distinguishable in the protocol" — by extending the existing `realtime-gateway.service.spec.ts` with a `connection versus subscription protocol` describe (2 cases): no envelope on `handleConnection` (transport-level connection success), one `ack` with `payload.action === 'subscribed'` on a valid subscribe, and a single topic-attributed `error` envelope with the socket left open on rejected auth.
- Files changed: `packages/twenty-server/src/engine/core-modules/realtime-gateway/services/__tests__/realtime-gateway.service.spec.ts`, `docs/plan/phases/phase-02-report.md`, `.ralph-tui/progress.md`.
- **Learnings:**
  - `npx nx lint:diff-with-main twenty-server` resolves files from `git diff main...HEAD`, so uncommitted edits produce "No changed files." — for uncommitted work run `npx oxlint --type-aware -c .oxlintrc.json <files>` + `npx oxfmt --check <files>` directly (phase-report precedent).
  - twenty-server jest enables fake timers globally; flush async handler continuations with `await Promise.resolve()` loops, not `setImmediate` (added to Codebase Patterns).
  - `handleConnection` is the public unit seam; an `EventEmitter` socket with `readyState = 1` lets a spec drive the real subscribe/error envelope path without a ws handshake.
---

## 2026-09-18 - P3.2-revision-history
- Implemented the durable restore-as-new-revision leg: added `EditorVersionHistoryStore.addRestoreSnapshots({ currentBody, restoredBody })` (appends both bodies through `addSnapshot`, reusing the single persistence/prune seam) and called it from `BlockEditorVersionHistoryPanel.handleRestore` before `editor.replaceBlocks`. Previously a restore was only recorded when the 120s interval snapshot eventually fired, so a reload right after a restore could lose the restore as a revision.
- Files changed: `packages/twenty-front/src/modules/blocknote-editor/version-history/EditorVersionHistoryStore.ts`, `.../components/BlockEditorVersionHistoryPanel.tsx`, `.../__tests__/EditorVersionHistoryStore.test.ts` (21 → 25 cases), `docs/plan/phases/phase-03-report.md`, `.ralph-tui/progress.md`.
- **Learnings:**
  - The persistence (2026-09-17) and in-memory version-history/diff/restore (2026-09-10) were already done-for-review; the remaining acceptance gap in the bullet was restore durability — the interval snapshot cadence (5s calm, 120s min interval) means a restore is not a revision until minutes later.
  - Keep one history seam: `addRestoreSnapshots` delegates to `addSnapshot`; `getBlockLevelDiff` stays the diff source and `replaceBlocks` the apply primitive.
  - `npx nx lint:diff-with-main twenty-front` compares `main...HEAD`, so uncommitted edits report "No changed files." — run `npx oxlint --type-aware -c .oxlintrc.json <files>` + `npx oxfmt --check <files>` directly (phase-report precedent).
  - `oxfmt` lives at repo root and `packages/twenty-front/.oxlintrc.json` is the front package config (root has no `.oxlintrc.json`).
---

## 2026-09-18 - P3.2-atomic-save
- No implementation needed: the atomic expected-revision save slice was authored and committed under `5868a0e9 save` and reported done-for-review 2026-09-17 11:41 UTC (phase-03-report line 716). Re-verified it end-to-end with zero source changes: 24/24 co-editing tests, 151/151 related tests, tsgo clean, oxlint+oxfmt clean on the 4 seam files. Only the Tier-2 two-session browser proof remains (orchestrator-owned).
- Files changed: `docs/plan/phases/phase-03-report.md`, `.ralph-tui/progress.md` (report-only iteration).
- **Learnings:**
  - The client-side guard already covers the acceptance's "exactly one winner / one conflict, loser draft survives" at unit level: `useDocumentSaveConflictGuard` withholds persist on an unresolved conflict while `persistBlocknoteBody` is the single normal+keep-local write seam in `RichTextFieldEditor.tsx`.
  - Explicit design choice: the expected revision is the block-diff against a base body, not a server revision token — PLAN P3.2 forbids a server save/merge protocol and OT/CRDT in v1. Do not "fix" this by adding a compare-and-set server endpoint.
  - Repeat assignment happens when the orchestrator has not ticked PLAN.md (still `[ ]` because only Tier 2 is missing); a re-verify + done-for-review report is the correct response, never a re-implementation.
---

## 2026-09-18 - P1.6c-front-onboarding
- Implemented the "empty catalogue cannot discard template choice" acceptance gap: the A2E onboarding step hosts the workspace-template picker on the same page as app install, but the native empty-catalogue auto-skip ran before the picker could render, silently discarding the choice. The page now only auto-skips when there is nothing to choose (0 available apps AND no template choices).
- Files changed: `packages/twenty-front/src/modules/onboarding/utils/shouldAutoSkipInstallAppsStep.ts` (new), `.../utils/__tests__/shouldAutoSkipInstallAppsStep.test.ts` (new, 5 cases), `packages/twenty-front/src/pages/onboarding/InstallApps.tsx` (hoist picker + util call), `docs/plan/phases/phase-01-report.md`, `.ralph-tui/progress.md`.
- **Learnings:**
  - The install-apps onboarding step carries the A2E template picker; the auto-skip short-circuit is the concrete way "empty catalogue discards template choice" manifests. Keep the guard parameterized on `hasTemplateChoices` so native auto-skip survives for callers without a picker.
  - Existing P1.6c preview/exclusion/retry flow (2026-09-14 + wire-enum fix 2026-09-17) is working — do not rebuild it; this slice only adds the missing guard + unit coverage.
  - `npx nx lint:diff-with-main twenty-front` compares `main...HEAD` and reports "No changed files" for uncommitted work; run `npx oxlint --type-aware -c .oxlintrc.json <files>` + `npx oxfmt --check <files>` directly from `packages/twenty-front`.
---

## 2026-09-18 - P1.6e-workspace-reuse
- No source change: the story was re-queued after the orchestrator had not ticked PLAN.md, and every executor-runnable (Tier 0/1) leg was already implemented and committed — gallery entrypoint (2026-09-16), first-open entrypoint (2026-09-17), save/duplicate/delete payload helpers and the browser buttons (2026-09-14). Re-verified the committed a2e-documents work end-to-end and reported `done-for-review`, mirroring the P3.2-atomic-save precedent.
- Files changed: `docs/plan/phases/phase-01-report.md`, `.ralph-tui/progress.md` (report-only iteration).
- **Learnings:**
  - The only P1.6e acceptance leg left is the Tier-2 populated-workspace/browser regression (install a2e-documents on a customized workspace, then save→template / gallery + first-open instantiate / delete-template and assert copies + existing fields/views/nav/roles survive). Prior P1.6e entries (2026-09-14/16/17) all recorded the same gap; it is orchestrator-only.
  - "Delete template without deleting copies" needs no code: the copy payload key set is exactly title/kind/position/content, so a copy has no template id and no FK back to the template — deletion cannot cascade. The existing lib suite already proves this plus edit-isolation.
  - When a re-queued story is exhausted for Tier 0/1, the correct move is a re-verify + `done-for-review` report with zero source changes — never re-implement or invent a descriptor loader (the 05-template-contracts §8 `TEMPLATE_CONTENT_*` codes stay document-only).
---
